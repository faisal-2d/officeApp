// gtm.js

import TagManager from 'react-gtm-module';

const initializeGTM = () => {
  TagManager.initialize({
    gtmId: 'GTM-NFMZK5LC', // Replace with your GTM container ID
  });
};

export default initializeGTM;
